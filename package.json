{
  "name": "flagplan",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "db:generate": "prisma generate",
    "db:migrate": "node --no-warnings scripts/setup-db.js && prisma generate",
    "db:setup": "node --no-warnings scripts/setup-db.js && prisma generate",
    "db:push": "prisma db push",
    "db:studio": "prisma studio",
    "demo": "node scripts/seed-demo.js",
    "auth:hash": "node scripts/hash-password.js"
  },
  "dependencies": {
    "@prisma/client": "^6.14.0",
    "bcryptjs": "^3.0.3",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "express-session": "^1.19.0",
    "lucide-static": "^1.11.0",
    "nanoid": "^5.0.9",
    "passport": "^0.7.0",
    "passport-local": "^1.0.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "prisma": "^6.14.0"
  },
  "engines": {
    "node": ">=22"
  }
}
